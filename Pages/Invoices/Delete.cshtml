@page
@model Proiect.Pages.Invoices.DeleteModel

<h2>Delete Invoice</h2>

<form method="post">
    <input type="hidden" asp-for="Invoice.ID" />
    <div>
        <h4>Are you sure you want to delete this invoice?</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Invoice.TotalAmount)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Invoice.TotalAmount)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Invoice.AppointmentID)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Invoice.AppointmentID)
            </dd>
            <!-- Additional details can be added here -->
        </dl>
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="./Index">Back to List</a>
    </div>
</form>

