@page
@model Proiect.Pages.Appointments.DeleteModel

<h2>Delete Appointment</h2>

<form method="post">
    <input type="hidden" asp-for="Appointment.ID" />
    <div>
        <h4>Are you sure you want to delete this appointment?</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Appointment.DateTime)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Appointment.DateTime)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Appointment.ReasonOfVisit)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Appointment.ReasonOfVisit)
            </dd>
            <!-- Repeat for other Appointment properties -->
        </dl>
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="./Index">Back to List</a>
    </div>
</form>

